# +required Version of the endpoint configuration YAML
version: 1.1

- name: post-extractor
  service:
     port: 8000
  type: REST
    - name: post-extractor
      displayName: post-extractor
      service:
        basePath: /
        port: 8000
      type: REST
      networkVisibilities:
        - Public
      schemaFilePath: .
